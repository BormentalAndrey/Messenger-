# Уже есть
android.useAndroidX=true
android.enableJetifier=true
kotlin.code.style=official

# Добавьте эти строки — они решают OutOfMemoryError при dexing
org.gradle.jvmargs=-Xmx4g -Dkotlin.daemon.jvm.options="-Xmx4g"
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# Опционально — подавить предупреждение о compileSdk=35 (пока AGP 8.4.2 не поддерживает официально)
android.suppressUnsupportedCompileSdk=35
